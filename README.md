# clinic-appointment-code-test

## Quickstart

1. Install dependencies
    ```shell script
   docker-compose run api yarn install
    ```
   
2. Start the application containers
    ```shell script
   docker-compose up
    ```
